# Reproducibility Notes

- Keep SD1.5 and SD3 environments separate.
- Install local modified diffusers with editable mode in each implementation.
- Use project-level environment variables for paths instead of hard-coded absolute paths.

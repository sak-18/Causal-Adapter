# Work Log - 2026-05-26

## Today Completed

- Completed non-notebook hardcoded path portability cleanup across the monorepo.
- Refactored high-impact benchmark entrypoints and metrics modules to remove machine-specific paths:
  - `counterfactual-benchmark/counterfactual_benchmark/methods/deepscm/*`
  - `counterfactual-benchmark/counterfactual_benchmark/evaluation/metrics/*`
- Refactored benchmark dataset/embedding modules for repository-relative defaults:
  - `counterfactual-benchmark/counterfactual_benchmark/ctf_datasets/*`
  - `counterfactual-benchmark/counterfactual_benchmark/evaluation/embeddings/embeddings.py`
- Refactored SD1.5/SD3 core scripts and helpers for portability:
  - `causal-adapter-sd15/MCPL_linear*.py`, `causal-adapter-sd15/causalnet*.py`
  - `causal-adapter-sd3/MCPL_linear*.py`, `causal-adapter-sd3/causalnet*.py`
  - `causal-adapter-sd3/causal_modules/{__init__.py,ddim_modules.py,ddim_modules_flux.py}`
- Normalized YAML/TXT/JSON template paths to placeholder variables.
- Updated README styling with light emoji enhancements.
- Updated migration notes to reflect current cleanup status.

## Commits / Sync

- `6095d5c` chore: complete non-notebook path portability cleanup
- `cc8720a` docs: add migration notes and remaining path-fix roadmap
- `4f2c026` refactor: make key sd15/sd3 entry scripts path-portable
- Pushed to `origin/main`.

## Current Repo State

- Branch: `main`
- Working tree: clean (no staged or unstaged changes at log time)
- Remaining hardcoded-path footprint: mainly notebook files (`.ipynb`)

## Next Steps

1. Notebook cleanup pass
- Replace remaining machine-specific paths in notebook code cells/markdown.
- Optionally clear notebook outputs and transient metadata for cleaner open-source release.

2. Smoke verification
- Run one minimal command path per module:
  - SD1.5: train/inference dry-run argument check
  - SD3: train/inference dry-run argument check
  - Benchmark: one metric/eval entrypoint sanity run

3. Release hygiene
- Re-check `.gitignore` coverage for generated artifacts and checkpoints.
- Re-scan for secrets/endpoints and legacy private URLs.

4. Final release prep
- Update top-level docs/changelog with final cleanup summary.
- Tag a release checkpoint once smoke checks pass.
